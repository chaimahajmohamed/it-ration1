{% extends 'base.html.twig' %}

{% block body %}
    <h1>{{ service.libelleService }}</h1>

    <table class="table table-bordred">
        <tbody>
        <tr>
            <th>Image</th>
            <td><img src="{{asset('images/')}}{{ service.imageservice}}" style="width: 100px;height: 100px"></td>
        </tr>
            <tr>
                <th>Id</th>
                <td>{{ service.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ service.libelleService }}</td>
            </tr>
            <tr>
                <th>Description:</th>
                <td> {{ service.descriptionService }}</td>
            </tr>

        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('admin_index') }}">Retourner à la liste</a>
        </li>
        <li>
            <a href="{{ path('admin_edit', { 'id': service.id }) }}">Modifier</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Supprimer">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
